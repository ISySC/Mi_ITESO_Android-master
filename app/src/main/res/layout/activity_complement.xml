<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff">

    <include
        android:id="@+id/ly_header"
        layout="@layout/layout_header" />

    <RelativeLayout
        android:id="@+id/ln_itesubes"
        android:layout_width="match_parent"
        android:layout_height="71dp"
        android:layout_below="@id/ly_header"
        android:background="@drawable/background_border">

        <ImageView
            android:id="@+id/ic_itesubes"
            android:layout_width="53dp"
            android:layout_height="53dp"
            android:layout_marginBottom="17dp"
            android:layout_marginLeft="18dp"
            android:layout_marginTop="9dp"
            android:src="@drawable/ic_logo_itesubes" />

        <TextView
            android:id="@+id/lbl_itesubes_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginLeft="30dp"
            android:textColor="@color/colorTxtGeneral"
            android:textSize="@dimen/font_size_12"
            android:layout_toRightOf="@id/ic_itesubes"
            android:text="@string/itesubes_title" />

    </RelativeLayout>

</RelativeLayout>
